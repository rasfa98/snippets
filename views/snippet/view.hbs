<h1>{{title}}</h1>
<div class="card">
    <div class="card-body">
        <h2 class="view">{{body}}</h2>
        <p>Tags</p>
        {{#each tags}}
        <a class="btn btn-outline-primary btn-sm">tags</a>
        {{/each}}
    </div>
</div>